- Add scope for typing emualtion too
- autorouting where possible (still give a fallback if needed)
- See if it can built auto if binary not availble for that OS like it does in maturin. Or to tackle this we could just bundle with as much as possible taget binaries.
- Add test cases to check if the wasm and node part work properly (not all test cases have to checked, just one complex one)

## Build and packaging, try to package for max possible
