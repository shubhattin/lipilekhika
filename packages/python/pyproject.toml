[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "lipilekhika"
version = "0.0.0a1"
description = "Lipi Lekhika is a library for transliterating text from one script to another."
readme = "README.md"
license = "MIT"
requires-python = ">=3.10"
dependencies = []

[tool.hatch.build.targets.wheel]
packages = ["lipilekhika"]
