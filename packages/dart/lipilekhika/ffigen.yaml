# Run with `dart run ffigen --config ffigen.yaml`.
name: LipilekhikaBindings
description: |
  Bindings for `src/lipilekhika.h`.

  Regenerate bindings with `dart run ffigen --config ffigen.yaml`.
output: 'lib/lipilekhika_bindings_generated.dart'
headers:
  entry-points:
    - 'src/lipilekhika.h'
  include-directives:
    - 'src/lipilekhika.h'
preamble: |
  // ignore_for_file: always_specify_types
  // ignore_for_file: camel_case_types
  // ignore_for_file: non_constant_identifier_names
comments:
  style: any
  length: full
